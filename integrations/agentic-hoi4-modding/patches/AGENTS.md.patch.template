# Apply after reading the live generic repository AGENTS.md.
# This is an anchor-based patch template because the repository URL and current file were unavailable during planning.

## Repo skill routing addition

Add beside the repository's visual-asset or portrait skill bullets:

- Use `hoi4-portrait-pipeline` after an attributed real-person source and exact crop exist. It owns the standalone ComfyUI Krea 2 production contract, independent identity audit gate, deterministic 156x210 output, DDS validation, and portrait handoff. The repository's existing asset skill remains the source of truth for source classification, runtime paths, GFX ownership, and live-consumer validation.

## Subagent routing addition

Add beside current asset and audit subagent rules:

- For real-person portraits, route attributed source research and exact crop evidence to the repository's sourced-asset role, bounded production to `hoi4_portrait_pipeline`, and independent read-only review to `hoi4_portrait_identity_auditor`. The producer cannot approve itself. The parent owns final character, GFX, gameplay-path, live-consumer, and in-game validation.

## Visual-asset ownership addition

Add to the visual-asset section:

For a real-person country-leader, commander, operative, or named-officeholder portrait, the sourced-asset role stops after the immutable source and exact crop handoff. The portrait producer creates candidates and production evidence. The independent portrait auditor returns schema-valid PASS, FAIL, or UNCERTAIN verdicts without ranking candidates. No DDS may be wired before an independent all-PASS audit.

## Infrastructure rule

Add once, near MCP or external-tool guidance:

ComfyUI MCP and API guidance belongs in `hoi4-portrait-pipeline` and the standalone portrait project. Do not create a broad central MCP skill for this integration. Use only actual discovered MCP tools and schemas. Keep raw ComfyUI private and expose remote work only through an authenticated narrow adapter.
