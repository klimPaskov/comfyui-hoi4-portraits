The source, processing, and text-to-image workflows use six steps, CFG 1, and
FLUX guidance 1. Image-edit passes use denoise 1.00, and portrait LoRA strength
defaults to 1.00. Restoration keeps its fixed seed and remains disabled until
enabled with its red toggle.

Run this in the RunPod web terminal:

```bash
export HF_TOKEN="hf_..."
P=/workspace/comfyui-hoi4-portraits
COMFY_ROOT=/workspace/runpod-slim/ComfyUI
test -f "$COMFY_ROOT/main.py" || { echo "ComfyUI not found at $COMFY_ROOT; set COMFY_ROOT to the folder containing main.py."; exit 1; }
if test -d "$P/.git"; then git -C "$P" pull --ff-only; else git clone --depth 1 https://github.com/klimPaskov/comfyui-hoi4-portraits.git "$P"; fi
"$P/scripts/install_runpod.sh" "$COMFY_ROOT"
```

Restart ComfyUI, close older workflow tabs, and reopen the workflow from
**Workflows → hoi4_portraits**.
