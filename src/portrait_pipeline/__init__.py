"""HOI4 portrait pipeline.

The package is deliberately fail-closed: an artifact is never promoted to a
final DDS or mod integration path without the recorded gates and an
independent audit.
"""

__version__ = "0.1.0"

